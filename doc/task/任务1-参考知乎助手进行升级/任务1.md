# 任务1

[知乎助手项目](D:\win_www\zhihuhelp)从b0b89997dc64f2f6b184c49bc3a7df1dde093d02提交以来一直未动，前段时间完成了到commitid`352c5837ed6634e26ff3671d51e5a49d8f7bc3c6`的一系列提交，大致变更位于[changelog](D:\win_www\zhihuhelp\changelog.md) 的3.0.0部分

稳部落(stablog)项目架构与知乎助手类似，希望参考知乎助手项目，拟定升级计划

注意: 项目没有历史依赖，本次迭代完之后预计不会有需求变更。所以1. 不需要对历史做兼容 2. 期望这次能在软件工程上达到完美

## 待确认项

### 1. 升级计划的功能范围

@todo(worker) 知乎助手 3.0.0 同时包含通用工程能力（ESM、统一 workflow、运行上下文、结构化日志、测试体系、开发文档、依赖与发布升级）和产品功能（Markdown 输出、数据浏览、缓存导入导出、输出历史、自动识别任务、官网、版本检查、埋点等），其中部分功能与知乎的数据模型或产品形态强相关。请确认稳部落本次计划是：完整移植所有可适配能力，还是仅升级工程基础并另行选择产品功能。该选择会显著影响开发阶段、数据/API 设计和验收范围。推荐以“通用工程能力全部对齐，产品功能逐项评估后纳入”为原则，并明确列出本次必须交付的产品功能。

@todo(codex) 稳部落本次升级目标为


### 2. 数据与输出能力边界

@todo(worker) 请确认是否要求把知乎助手 3.0.0 的 Markdown 输出、缓存 JSON 全库/增量导入导出、数据浏览页和输出历史全部适配到微博数据模型。它们会引入新的输出契约、导入冲突规则、前后端 IPC/API 和较大测试范围。推荐全部纳入，以匹配“没有历史兼容负担、一次性完善工程质量”的目标；导入时同 ID 数据按业务更新时间保留较新版本，并使用隔离 fixture 验收，不接触现有业务数据库。

### 3. 官网、更新检查、遥测与许可发布范围

@todo(worker) 请确认本次是否包含官网/下载页、版本接口与应用内更新检查、匿名埋点，以及 MIT/GPL 双许可和第三方依赖/对应源码材料。上述内容涉及仓库外发布渠道、隐私口径和许可策略，不能仅依据“参考知乎助手”自动决定。推荐本次纳入版本检查和完整发布/许可校验；官网作为独立阶段；匿名埋点默认不纳入，除非明确数据字段、用户告知与关闭方式。

### 4. “软件工程上达到完美”的验收标准

@todo(worker) 请确认可执行的质量门槛。推荐定义为：Node.js 24、pnpm 11、Electron 43、原生 ESM；主进程/CLI/GUI 复用 workflow；统一 schema、错误与结构化日志；单元测试、前端组件测试、离线集成测试和隔离 fixture；Windows/macOS 构建及最小在线 Electron 冒烟测试；关键业务路径和失败路径均有测试；补齐架构、数据、IPC、日志、测试、发布和故障排查文档；所有检查在干净环境通过，且测试不读取或覆盖现有业务数据。若平台、版本或覆盖范围不同，请给出目标。

