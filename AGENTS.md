# stablog 仓库协作约定

## 需求门禁

- 开始任何代码修改前，必须先阅读用户需求、相关任务文档和现有实现，判断信息是否足够完成全量开发。
- 若存在会影响范围、接口、数据、兼容性或验收结果的实质性歧义，优先在当前任务文档中补充确认项；若当前需求没有任务文档，则使用 `doc/task` 下一个未占用编号创建 `问题描述-N/问题N.md`。
- 待确认项使用 `@todo(worker)` 标注，必须说明待决策内容、影响和推荐方案。用户使用 `@todo(codex)` 回复。
- 收到回复前不得开始业务代码开发。确认完成后，应把结论合并为正式需求，并删除对应的 `@todo(worker)` 与 `@todo(codex)` 问答，再开始开发。
- 信息已经充分时，不创建重复的任务文档。

## 文档同步

- 修改行为、架构、命令、IPC/API、数据结构、持久化、日志或主要业务流程时，必须在同一次变更中更新 `doc/dev` 或当前任务文档。
- 文档应描述最终有效行为，不保留已经失效的讨论结论或已完成 TODO。
- 测试命令、运行前提、fixture 更新方式和已接受的兼容性边界发生变化时，必须同步更新测试说明。

## 开发与验收

- 只修复当前需求和测试直接暴露的真实缺陷，避免无关重构。
- 保持 `pnpm start` 只启动已有 `dist/index.js`；开发期间由 `pnpm watch` 独立更新 `dist`。
- 完成前运行与改动范围匹配的类型检查、测试和构建，并确认测试不读取或覆盖现有业务数据。
